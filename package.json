{
  "name": "payload-blog",
  "description": "Blog template",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon",
    "build": "payload generate:types && payload build",
    "serve": "NODE_ENV=production node server.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.171.0",
    "@payloadcms/plugin-cloud-storage": "^1.0.9",
    "@payloadcms/plugin-seo": "^1.0.8",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "payload": "^1.1.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
